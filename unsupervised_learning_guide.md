# 🤖 비지도학습(Unsupervised Learning) 완전 정리

## 📖 목차
- [비지도학습이란?](#비지도학습이란)
- [핵심 특징](#핵심-특징)
- [비지도학습의 유형](#비지도학습의-유형)
- [주요 알고리즘](#주요-알고리즘)
- [실생활 활용 사례](#실생활-활용-사례)
- [자율주행 활용 사례](#자율주행-활용-사례)
- [장점과 단점](#장점과-단점)
- [다른 학습 방법과의 비교](#다른-학습-방법과의-비교)
- [시작하기](#시작하기)

## 비지도학습이란?

**비지도학습(Unsupervised Learning)**은 정답(라벨)이 없는 데이터에서 숨겨진 패턴이나 구조를 스스로 찾아내는 머신러닝 방법입니다. 알고리즘이 데이터를 탐색하며 자동으로 의미 있는 패턴, 관계, 구조를 발견합니다.

### 🎯 핵심 개념
- **입력**: 라벨이나 정답이 없는 원시 데이터
- **목표**: 숨겨진 패턴, 구조, 관계 발견
- **출력**: 데이터 구조에 대한 통찰, 군집, 이상치

### 💡 쉬운 비유
비지도학습은 범죄 수사관이 어떤 범죄가 일어났는지 모르는 상태에서 증거들을 조사하는 것과 같습니다. 
단서들 사이의 패턴, 연결점, 이상한 점들을 찾아서 숨겨진 이야기를 밝혀내는 것이죠.

## 핵심 특징

| 특징 | 설명 |
|------|------|
| **정답 없음** | 라벨이 없는 데이터로 작업 - "정답"이 주어지지 않음 |
| **패턴 발견** | 데이터 속 숨겨진 구조와 관계를 찾아냄 |
| **탐색적** | 데이터 탐색과 이해를 위해 사용 |
| **자기주도** | 외부 가이드 없이 알고리즘이 스스로 학습 |
| **무감독** | 선생님이나 감독자가 피드백을 주지 않음 |

## 비지도학습의 유형

### 1. 📊 군집화(Clustering)
**목적**: 비슷한 데이터들을 그룹으로 묶기

**작동 방식**: 
- 데이터의 특징을 분석해서 유사성 찾기
- 관련된 데이터들의 그룹(클러스터) 생성
- 같은 클러스터 내 데이터는 서로 다른 클러스터 데이터보다 더 유사

**예시**: 구매 행동을 기반으로 한 고객 세분화

### 2. 📏 차원축소(Dimensionality Reduction)
**목적**: 중요한 정보는 보존하면서 데이터를 단순화

**작동 방식**:
- 특징/변수의 개수를 줄임
- 가장 중요한 패턴만 유지
- 데이터를 시각화하고 처리하기 쉽게 만듦

**예시**: 1000개 특징의 이미지 데이터를 10개 핵심 특징으로 압축

### 3. 🔍 연관 규칙 학습(Association Rule Learning)
**목적**: 서로 다른 항목들 간의 관계 찾기

**작동 방식**:
- "A이면 B이다" 같은 규칙 발견
- 자주 함께 나타나는 패턴 식별
- 데이터 속 숨겨진 연결고리 발견

**예시**: "빵을 사는 사람은 버터도 산다"

### 4. ⚠️ 이상 탐지(Anomaly Detection)
**목적**: 비정상적이거나 unusual한 데이터 포인트 식별

**작동 방식**:
- "정상적인" 데이터가 어떤 모습인지 학습
- 패턴에 맞지 않는 데이터 포인트 표시
- 이상치, 사기, 오류 식별에 도움

**예시**: 신용카드 사기 거래 탐지

## 주요 알고리즘

### 군집화 알고리즘

| 알고리즘 | 적합한 경우 | 장점 | 단점 |
|----------|------------|------|------|
| **K-Means** | 구형 클러스터 | 빠르고 간단 | 클러스터 개수를 미리 정해야 함 |
| **계층적 군집화** | 트리 구조 관계 | 클러스터 개수 지정 불필요 | 대용량 데이터에서 느림 |
| **DBSCAN** | 불규칙한 모양, 노이즈 처리 | 임의의 모양 발견 | 매개변수에 민감 |
| **가우시안 혼합** | 겹치는 클러스터 | 확률론적 접근 | 가우시안 분포 가정 |

### 차원축소

| 알고리즘 | 적합한 경우 | 설명 |
|----------|------------|------|
| **PCA** | 선형 관계 | 주성분 분석 - 분산의 주요 방향 찾기 |
| **t-SNE** | 시각화 | 고차원 데이터를 2D/3D로 시각화하기 좋음 |
| **UMAP** | 대용량 데이터 | t-SNE보다 빠르고 전역 구조 보존 |
| **ICA** | 신호 분리 | 독립성분 분석 - 혼합된 신호 분리 |

### 연관 규칙

| 알고리즘 | 사용 사례 | 설명 |
|----------|----------|------|
| **Apriori** | 장바구니 분석 | 빈번한 아이템 조합 찾기 |
| **FP-Growth** | 대용량 데이터 | Apriori보다 빅데이터에서 빠름 |

## 실생활 활용 사례

### 🛒 전자상거래 & 마케팅
- **고객 세분화**: 행동별 고객 그룹화
- **추천 시스템**: "X를 산 고객은 Y도 샀습니다"
- **장바구니 분석**: 상품 배치 최적화
- **가격 최적화**: 수요 패턴 기반 동적 가격 책정

### 🏥 의료
- **질병 군집화**: 비슷한 증상의 환자 그룹화
- **신약 개발**: 분자 구조에서 패턴 찾기
- **의료 영상 분석**: 스캔에서 이상 징후 탐지
- **전염병 추적**: 질병 확산 패턴 식별

### 💰 금융
- **사기 탐지**: 비정상적인 거래 패턴 식별
- **위험 평가**: 위험 프로필별 고객 그룹화
- **알고리즘 트레이딩**: 시장 패턴 발견
- **신용 평가**: 라벨 없이 신용도 평가

### 🌐 기술
- **네트워크 보안**: 사이버 공격 탐지
- **소셜 미디어 분석**: 사용자 커뮤니티 발견
- **검색 엔진**: 콘텐츠 정리 및 순위 매기기
- **데이터 압축**: 품질 보존하며 파일 크기 줄이기

## 자율주행 활용 사례

### 🚗 교통 패턴 인식
```
시나리오: 미리 정의된 카테고리 없이 운전 행동 분석
- 입력: 속도, 핸들 각도, 가속도 데이터
- 알고리즘: K-Means 클러스터링
- 출력: 발견된 패턴 (고속도로, 시내, 주차)
- 이점: 지역별 운전 스타일에 자동 적응
```

### 🛣️ 경로 최적화
```
시나리오: GPS 라벨 없이 효율적인 경로 찾기
- 입력: 과거 여행 데이터, 교통 패턴
- 알고리즘: 계층적 군집화
- 출력: 자연스러운 경로 그룹화
- 이점: 사람이 라벨링하지 않은 지름길 발견
```

### ⚠️ 이상 상황 탐지
```
시나리오: 위험한 상황 식별
- 입력: 센서 데이터 (카메라, 라이다, 레이더)
- 알고리즘: Isolation Forest, One-Class SVM
- 출력: 위험을 나타낼 수 있는 비정상 패턴
- 이점: 훈련 데이터에 없는 새로운 위험 유형 탐지
```

### 🎯 센서 융합
```
시나리오: 서로 다른 센서 입력 결합
- 입력: 여러 센서 스트림
- 알고리즘: PCA, 독립성분 분석
- 출력: 축소된 의미 있는 특징
- 이점: 복잡한 센서 데이터에서 핵심 정보 추출
```

### 👥 운전자 행동 분석
```
시나리오: 개별 운전 스타일 학습
- 입력: 핸들, 브레이킹, 가속 패턴
- 알고리즘: 가우시안 혼합 모델
- 출력: 개인별 운전 프로필
- 이점: 선호도에 맞춰 자율주행 행동 커스터마이징
```

## 장점과 단점

### ✅ 장점

| 장점 | 설명 |
|------|------|
| **라벨링 불필요** | 원시 데이터로 작업 - 비싼 주석 작업 불필요 |
| **패턴 발견** | 사람이 놓칠 수 있는 예상치 못한 관계 발견 |
| **확장 가능** | 대량의 데이터를 자동으로 처리 |
| **탐색적** | 다른 방법 적용 전 데이터 이해에 좋음 |
| **비용 효율적** | 수동 데이터 준비 필요성 감소 |

### ❌ 단점

| 단점 | 설명 |
|------|------|
| **명확한 목표 없음** | "성공"이 무엇인지 정의하기 어려움 |
| **검증 어려움** | 성능을 측정할 정답이 없음 |
| **해석 어려움** | 결과를 이해하고 설명하기 어려울 수 있음 |
| **매개변수 민감** | 많은 매개변수 조정이 필요한 경우가 많음 |
| **주관적 결과** | 같은 데이터도 다른 유효한 해석 가능 |

## 다른 학습 방법과의 비교

| 측면 | 지도학습 | 비지도학습 | 강화학습 |
|------|----------|------------|----------|
| **데이터 유형** | 라벨된 데이터 (입력-출력 쌍) | 라벨 없는 데이터 | 환경과의 상호작용 |
| **목표** | 정확한 출력 예측 | 숨겨진 패턴 발견 | 누적 보상 최대화 |
| **피드백** | 즉각적인 정답 | 피드백 없음 | 지연된 보상/처벌 |
| **예시** | 이메일 스팸 탐지, 이미지 분류 | 고객 세분화, 이상 탐지 | 게임 플레이, 로봇 제어 |
| **평가** | 명확한 지표 (정확도, 정밀도) | 주관적 또는 도메인별 | 보상 기반 지표 |

## 시작하기

### 🛠️ 필수 도구

```python
# 비지도학습을 위한 핵심 라이브러리
import numpy as np                 # 수치 계산
import pandas as pd               # 데이터 조작
import matplotlib.pyplot as plt   # 시각화
import seaborn as sns            # 통계 시각화

# Scikit-learn 알고리즘
from sklearn.cluster import KMeans, DBSCAN
from sklearn.decomposition import PCA
from sklearn.preprocessing import StandardScaler
```

### 📚 학습 경로

#### 초급 (1-2개월)
1. **K-Means 클러스터링부터 시작**
   - 유사한 데이터를 그룹화하는 개념 이해
   - 간단한 데이터셋으로 연습 (iris, 고객 데이터)
   - 적절한 클러스터 개수 선택 방법 학습

2. **차원축소를 위한 PCA 시도**
   - 데이터 복잡성을 줄이는 방법 이해
   - 고차원 데이터 시각화 연습
   - 설명된 분산에 대해 학습

3. **기본 이상 탐지**
   - 데이터를 "비정상"으로 만드는 요소 이해
   - Isolation Forest 알고리즘 시도
   - 사기 탐지 예제로 연습

#### 중급 (2-4개월)
1. **고급 클러스터링 기법**
   - 계층적 클러스터링
   - 불규칙한 모양을 위한 DBSCAN
   - 가우시안 혼합 모델

2. **연관 규칙 학습**
   - 장바구니 분석
   - Apriori 알고리즘
   - 지지도, 신뢰도, 향상도 지표

3. **실제 프로젝트**
   - 고객 세분화
   - 이미지 압축
   - 소셜 네트워크 분석

#### 고급 (4개월 이상)
1. **딥 비지도학습**
   - 오토인코더
   - 변분 오토인코더 (VAE)
   - 생성적 적대 신경망 (GAN)

2. **전문 응용**
   - 시계열 클러스터링
   - 텍스트 마이닝과 토픽 모델링
   - 추천 시스템

### 🎯 빠른 시작 예제

```python
# 간단한 K-Means 클러스터링 예제
from sklearn.cluster import KMeans
from sklearn.datasets import make_blobs
import matplotlib.pyplot as plt

# 샘플 데이터 생성
X, _ = make_blobs(n_samples=300, centers=3, random_state=42)

# K-Means 적용
kmeans = KMeans(n_clusters=3, random_state=42)
labels = kmeans.fit_predict(X)

# 결과 시각화
plt.scatter(X[:, 0], X[:, 1], c=labels, cmap='viridis')
plt.title('K-Means 클러스터링 결과')
plt.show()
```

### 📖 추천 학습 자료

#### 도서
- **"패턴 인식과 기계 학습"** - Christopher Bishop
- **"통계 학습의 요소"** - Hastie, Tibshirani, Friedman
- **"핸즈온 머신러닝"** - 오렐리앙 제롱

#### 온라인 강의
- **Coursera**: Andrew Ng의 머신러닝 강의
- **edX**: MIT 머신러닝 입문
- **Udacity**: 머신러닝 엔지니어 나노디그리

#### 연습용 데이터셋
- **UCI 머신러닝 저장소**: 클래식 데이터셋
- **Kaggle**: 실제 경진대회 데이터셋
- **Scikit-learn**: 내장된 연습용 데이터셋

---

## 🎉 결론

비지도학습은 라벨된 예제 없이도 데이터에서 숨겨진 패턴을 발견하는 강력한 도구입니다. 지도학습보다 평가하기 어려울 수 있지만, 라벨된 데이터가 비싸거나 없을 때 독특한 통찰을 제공하고 문제를 해결할 수 있습니다.

**핵심 요점:**
- 🔍 데이터를 탐색해서 숨겨진 패턴 찾기
- 🚫 라벨이나 "정답" 불필요
- 🎯 데이터 탐색과 이해에 뛰어남
- 🚗 새로운 상황에 직면하는 자율 시스템에 필수
- 💡 지도학습과 강화학습 접근법을 보완

간단한 클러스터링과 차원축소부터 시작해서, 자신감과 이해를 쌓아가며 점진적으로 더 고급 기법들을 탐구해보세요!
