# 자율주행에서 파이썬 자료구조 사용법

## 📊 자료구조 비교표

| 자료구조 | 특징 | 사용 시기 | 자율주행 예시 |
|---------|------|----------|--------------|
| **딕셔너리** | • 서로 다른 종류의 데이터를 의미 있는 이름으로 묶어서 관리<br>• 관련 데이터를 묶어서 관리<br>• 코드 가독성이 좋아짐 | 서로 다른 종류의 데이터를 의미 있는 키로 구분할 때 | ```python<br>sensors = {"앞": 8.0, "왼쪽": 15.0, "오른쪽": 4.0}<br>``` |
| **리스트** | • 같은 종류의 데이터가 여러 개 있을 때 적합<br>• 순서가 중요할 때<br>• 같은 성격의 데이터 여러 개<br>• 인덱스로 접근 | 같은 종류의 데이터 여러 개를 순서대로 관리할 때 | ```python<br>tire_pressures = [2.2, 2.1, 2.3, 2.2]<br>``` |
| **튜플** | • 수정 불가능<br>• 순서 고정<br>• 실수 방지 - 중요한 설정값을 실수로 바꾸는 걸 막아줌 | 변하지 않는 고정된 데이터를 저장할 때 | ```python<br>vehicle_dimensions = (4.5, 1.8, 1.6)<br>``` |

## 🚗 자율주행 코드 예시

## 딕셔너리 코드 예시
```python
# 센서 데이터 (거리 측정값, 단위: 미터)
sensors = {
    "앞": 8.0,
    "왼쪽": 15.0,
    "오른쪽": 4.0
}

# 차량 상태 모니터링
vehicle_status = {
    "speed": 45.0,        # 속도 (km/h)
    "fuel": 87.3,         # 연료량 (%)
    "temperature": 89.5   # 온도 (℃)
}

# GPS 위치 데이터
gps_data = {
    "latitude": 37.5665,
    "longitude": 126.9780,
    "altitude": 85.2,
    "heading": 270.5  # 방향각 (도)
}
```

## 리스트 코드 예시
```python
# 4개 타이어 압력값들
tire_pressures = [2.2, 2.1, 2.3, 2.2]

# 연속된 거리 측정값들 (시간 순서대로)
distance_history = [8.2, 7.9, 7.5, 7.1, 6.8]

# 차선 좌표점들 (x, y 쌍)
lane_points = [(100, 200), (150, 195), (200, 190)]

# 장애물 위치들 (x, y 좌표)
obstacles = [(250, 180), (320, 175), (400, 190)]

# 신호등 위치들 (교차로별)
traffic_lights = [(500, 300), (800, 320), (1200, 350)]
```

## 튜플 코드 예시
```python
# 차량 규격 (길이, 너비, 높이) - 절대 변하지 않음
vehicle_dimensions = (4.5, 1.8, 1.6)

# GPS 좌표 (위도, 경도) - 한 번 기록하면 고정
destination = (37.5665, 126.9780)

# 카메라 해상도 (가로, 세로) - 설정값이라 고정
camera_resolution = (1920, 1080)

# 센서 설치 위치 (x, y, z) - 차량 제작시 고정
lidar_position = (0.0, 0.0, 1.5)
```

# 🔍 언제 어떤 자료구조를 사용하나?

## 딕셔너리 사용
- 서로 다른 종류의 데이터를 의미 있는 이름으로 구분할 때
- 센서 관련 정보들을 하나로 그룹화할 때
- 코드 가독성을 높이고 싶을 때

## 리스트 사용  
- 같은 종류의 데이터가 여러 개 있을 때
- 순서가 중요한 시간순 데이터, 좌표 순서
- 인덱스로 접근해야 할 때

## 튜플 사용
- 차량 사양, GPS 좌표, 화면 해상도처럼 절대 바뀌면 안 되는 값들
- 실수로 수정하면 시스템 오류가 날 수 있는 중요한 설정값들

## 💡 한 줄 정리
- **딕셔너리**: 서로 다른 종류 데이터를 이름으로 구분해서 하나로 묶음
- **리스트**: 같은 종류 데이터 여러 개를 순서대로 관리
- **튜플**: 절대 안 바뀌는 고정값 저장
